# 工作流多维度审查报告（复查 + 新变更回归）

- 日期: 2026-08-24
- 审查方式: 上次全量审查（audit-20260822-2106）修复项复查 + 新变更（UI 三件套入库、设计 skill 接入 vibe 工作流、CM11 提交规范）多维度回归，联动实测
- 范围: vibecoding-template 仓库 global/ 真源 + .opencode/quality-gate.js + scripts/check-sync.mjs + 新入库 3 个设计 skill
- 结论: **Minor**（上次 P0/P1/P2 修复全部落实，矩阵实测通过；本次新变更引入少量文档口径漂移 + 测试套件入库不完整，均不阻断工作流）

---

## 一、联动实测结果（阶段 0）

| # | 实测项 | 方法 | 结果 |
|---|--------|------|------|
| 0.1 | quality-gate 运行 | `node .opencode/quality-gate.js` | ✅ PASS，exit 0（通过 8，占位计数已清除） |
| 0.2 | 命令结构回归 | `checks.py` | ✅ PASS（口径"6 个 vibe-* 前缀命令 + DoD 模块"） |
| 0.3 | 漂移检测 S1–S7 | `node scripts/check-sync.mjs` | ✅ PASS（S7: 19 个 skill 引用扫 60 文件零孤儿/零缺失） |
| 0.4 | 镜像 vs 全局漂移 | 全量 SHA256 对比 global/ ↔ ~/.config/opencode/ | ✅ 126 文件完全一致，零漂移 |
| 0.5 | guard 防御矩阵 | `guard-matrix.mjs` | ✅ 误伤 0/16、漏网 0/22（shutdown/reboot 引号内文本已放行） |
| 0.6 | 密钥扫描矩阵 | `secret-matrix.mjs` | ✅ 误报 0/10、漏网 0/8 |
| 0.7 | 新 skill 可执行性 | `search.py --help` / `--design-system` 冒烟 + bash -n 两个脚本 | ✅ exit 0；`bash -n` 通过（但 CRLF 见 T-02） |
| 0.8 | ui-ux-pro-max 测试套件 | `python -m pytest scripts/tests/` | ❌ 2 收集错误 + 1 真实失败（见 T-01） |

---

## 二、上次审查修复项复查（P0/P1/P2：9 Minor + 16 Info）

| 报告编号 | 修复项 | 状态 | 证据 |
|---------|--------|------|------|
| B-01 | guard 关机正则收紧（仅命令首 token、排除引号内文本） | ✅ 已修复 | `guard/index.mjs:31` 正则 `(?:^|[;&\|]\s*)(?:sudo\|doas)?(?:shutdown\|reboot\|halt\|poweroff)(?:\.exe)?(?=\s\|$)`；实测 `npm run shutdown-handler-test`、`Write-Host "server will halt soon"` 均放行 |
| P-02/PH-02 | 防御矩阵落盘回归 | ✅ 已修复 | `guard-matrix.mjs`（16 合法 + 22 危险）、`secret-matrix.mjs`（10 合法 + 8 密钥样本）已入库并可直接运行 |
| C-01 | quality-gate M14/M15 占位假通过 | ✅ 已修复 | `quality-gate.js:23` 注释"历史遗留 M04/M05/M09/M14/M15 已从计量移除"；通过数 9→8 为真实检查项 |
| P-01 | 模板自身 PROTECTED_REGIONS 空数组 | ✅ 已修复 | `quality-gate.js:58-61` 已填入 `.opencode/quality-gate.js` + `starter-template/AGENTS.md` |
| C-02/PH-01 | quality-gate 空 catch（194 行） | ✅ 已修复 | `quality-gate.js:198-201` 现 push `pkg-parse-error` 阻断并显式报告，不再静默 |
| C-04 | sync-global.ps1 `-Exclude node_modules` 不生效 | ✅ 已修复 | `sync-global.ps1:64-68` 改为复制后递归删除目标 node_modules |
| D-01 | vault-sync 硬编码 vault 路径 | ✅ 已修复 | `vault-sync/index.mjs:11-32` 新增 `resolveVaultPath()`，运行时读 `opencode.json` 的 `references.vault.path`（homedir 拼接），配置不可读才回落硬编码默认值 |
| F-01 | 命令数量口径 5 处冲突 | ✅ 已统一 | 全仓库统一："7 个命令 = 6 个 `vibe-*` 前缀 + `vault-sync`"（vibe-README:23 明示）；checks.py 输出"6 个 vibe-*"口径一致 |
| F-02/D-04 | starter AGENTS 缺 /vibe-clean、04-CONTRACTS | ✅ 已修复 | `starter-template/AGENTS.md:18` 补 04-CONTRACTS、`:40` 补 /vibe-clean |
| S-01 | opencode.json 明文密钥措辞 | ✅ 已修复 | 根 AGENTS 与 starter 均保持"密钥不进前端/仓库、用环境变量"；opencode.json 本地明文密钥不进仓库（全局 README 有说明） |
| D-02 | 机器特定路径集中化 | ⚠️ 部分（未集中，新增 1 处硬编码，见 G-03） | 详见新发现 G-03 |

**结论：上次 9 项 Minor 全部修复（8 确认 + 1 部分），修复质量可控。**

---

## 三、新变更发现（2026-08-23~24 提交：README 门面 / CM11 提交规范 / UI 三件套入库 / 设计 skill 接入）

### 维度 1 — 功能完整性

- **T-01 (Minor 偏高，可升 Priority 1)** 新入库 `ui-ux-pro-max` 自带测试套件**不完整**：
  - `test_catalog_refresh.py:13-21` 与 `test_relevance_evaluator.py:9-14` 用 `next(parent...)` 向上查找**上游仓库才有**的脚本（`refresh-google-fonts.py`、`refresh-icon-catalog.py`、`evaluate-relevance.py`），本仓库 `scripts/` 只有 core/design_system/reasoning_contract/search/validate_data 5 个 → **收集即 StopIteration 报错**（2 文件无法运行）
  - **真实失败**：`test_data_contracts.py::TestGeneratedCatalogContract::test_canonical_catalogs_and_provenance_are_release_ready` FAILED —— `[catalog:summary] stale snapshot for phosphor-icons-upstream.json`（其余 129 passed + 7923 subtests passed）
  - 影响: 这是"防御机制写了但未测"的活证据——入库时把测试当资产带进来，却从未在模板仓库跑过（harness/check-sync/S7 都不执行 skill 内部测试）。过期快照意味着 `search.py` 引用的图标/字体 catalog 数据与源头脱节，新项目生成的设计令牌可能带过期条目。
  - 修复: ①剔除依赖上游私有脚本的 2 个测试文件（skill 内改成条件跳过并注释说明）；②为 `phosphor-icons-upstream.json` 刷新快照或将其契约测试标记 `@skipIf(stale)` 并记录 TODO；③在 `skills/ui-ux-pro-max/` 内补 README 说明本仓库测试运行方式（`python -m pytest scripts/tests/`）。

### 维度 2 — 文档同步

- **F-05 (Minor)** Skill 计数三处更新为 19，但 `docs/02-ARCHITECTURE.md` 目录树仍为 **16**：
  - `02-ARCHITECTURE.md:16` `skills/ # 16 个 Skill（5 方法 + coding-standards 族 11）`、`:47` `# 16 Skill 真源`——实际 5 方法 + 3 设计 + 11 族 = 19
  - README.md:1/82、docs/01-PRD.md:24、docs/03-STATUS.md 均已改 19 → ARCH 滞后
  - 修复: ARCH §2 目录树补 `frontend-design/`、`ui-ux-pro-max/`、`web-artifacts-builder/` 三行并改计数 19。

- **F-06 (Info)** `global/commands/vibe-README.md` "全局 skills" 清单（`vibe-README.md:70-71`）只列 6 个方法类 + coding-standards 族，**未列 3 个新设计类 skill**——新项目用户据此导航看不到视觉栈入口。
  - 修复: 补一行"设计类 3 个（frontend-design / ui-ux-pro-max / web-artifacts-builder）"。

- **F-07 (Info)** README badge 锚点悬空：`[![Skills](...skills-19-blue)](#skills-һ16-` 链接锚点还是 16 个旧标题，而正文标题已是 `## Skills 一览（19 个）` → 点击不跳转。
  - 修复: badge 锚点改 `#skills-一览-19-个`。

- **F-08 (Info)** `vibe-README.md:66` 引用的命令结构回归路径为机器特定 `C:\Users\fms\.claude\harness\...`——与 D-02/G-03 同类，集中到 AGENTS 常量区一并处理。

### 维度 3 — 代码/数据质量（新 skill 内部）

- **T-02 (Info)** `web-artifacts-builder/scripts/init-artifact.sh`（322 行）与 `bundle-artifact.sh`（53 行）**全部 CRLF 行尾**（实测 CRLF 计数 = 总行数）。Git bash `bash -n` 通过（Windows 下宽容解析），但 CRLF 在 shebang/heredoc/命令替换场景易出隐性问题，且破坏跨平台可移植性（与 DESIGN skill 是官方跨平台资产不符）。
  - 修复: 转 LF（`git add --renormalize` 或 `dos2unix`）并追加 `* text=auto eol=lf` 到 `.gitattributes`。

- **T-03 (OK)** `ui-ux-pro-max` 5 个 python 脚本 stdlib-only（无第三方依赖），core.py/design_system.py 通过 `test_core.py` 39 用例 + 冒烟生成 MASTER.md（设计系统输出正常）✅

### 维度 4 — 安全/防御机制

- **S-10 (OK)** 新技能入库无密钥：`LICENSE.txt` 均为上游许可文本，data CSV/JSON 无凭证样本；`secret-matrix.mjs` 扫描全仓库无新密钥 ✅
- **S-11 (OK)** guard-matrix 新增用例覆盖 shutdown.exe 变体、`sudo/doas` 前缀、`--force-with-lease` 放行——防御矩阵随攻击面扩展同步演进 ✅

### 维度 5 — 变更范围/提交卫生（CM11 提交规范相关）

- **G-01 (OK)** CM11 subject 重写采用 `git commit-tree`（tree 哈希 36/36 一致，仅 message 变），CHANGELOG 已声明 backup 分支 `backup/pre-cm11-rewrite`、`backup/pre-no-type`、`backup/pre-subject-rewrite`——备份分支本地存在（`git branch` 实测 3 个），历史可回溯，符合"有回滚路径" ✅
- **G-02 (Info)** 提交历史中出现同一变更的**双版本提交**（如 `a9a8625`/`5eb6db5`/`f9124aa` 等 content 相同、subject 不同——CM11 前无 type 与 CM11 后有 type 并存）。这是重写遗留，非错误，但新读者看 log 会用 content 差异困惑。已由 CHANGELOG 说明；若追求整洁可后续用 `git rebase --autosquash` 收敛（低优先，不改也可）。
- **G-03 (Info→Minor)** vibe-plan 阶段4 新增硬编码 `D:\Git\bin\bash.exe`（vibe-plan.md:79，web-artifacts-builder 启动脚本）——上一轮 D-02 建议"机器路径集中到 AGENTS 顶部常量区"未执行，本轮**新增一处**。SH13 虽解释了 WSL stub 陷阱（`C:\Windows\system32\bash.exe` 不可用），但模板应在命令中写语义（如"用 `Get-Command bash` 探测 Git bash 全路径"）而非散落绝对路径。
  - 修复: vibe-plan 阶段4 增加一行注意"Git bash 路径因机器而异：先 `Get-Command bash` 确认，WSL stub（system32 下）不可用"。

---

## 四、严重度汇总

| 严重度 | 数量 | 条目 |
|--------|------|------|
| **Blocking/Minor 偏高** | 1 | T-01 ui-ux-pro-max 测试套件不完整 + 数据快照过期（防御机制未验证 + 数据过期） |
| **Minor** | 2 | F-05 ARCH Skill 计数漂移（16 vs 19）、G-03 机器路径硬编码新增 |
| **Info** | 5 | F-06 vibe-README 缺设计 skill 清单、F-07 badge 锚点、F-08 机器路径、T-02 CRLF、G-02 双版本提交 |

## 五、修复优先级清单（本次只读未修改）

**P1（建议尽快）**
1. ui-ux-pro-max 测试套件三修：剔除依赖上游私有脚本的 2 个测试 / 刷新 phosphor 快照或挂 skip+TODO / 补测试运行说明 —— 修复 T-01
2. ARCH §2 目录树补 3 个设计 skill + 计数 16→19 —— 修复 F-05

**P2（文档健康/卫生）**
3. vibe-README 全局 skills 清单补设计类 3 个 —— 修复 F-06
4. README badge 锚点 16→19 —— 修复 F-07
5. web-artifacts-builder 脚本转 LF + .gitattributes 兜底 —— 修复 T-02
6. vibe-plan 阶段4 bash 路径语义化（Get-Command 探测 + WSL stub 警示）—— 修复 G-03

---

## 六、上次审查遗留（未完成项）

- D-02 "机器特定绝对路径集中化"：**未执行**，仅新增了 1 处硬编码（G-03）。建议下轮在 AGENTS.md 建立 `HOST_PATHS` 常量块统一声明（harness 路径 / Git bash / vault 路径），命令文件统一引用。

---

*报告生成: 2026-08-24 00:51 | 只读审查，无文件修改 | 临时冒烟目录已清理（系统 TEMP 下临时子目录，已删除）*