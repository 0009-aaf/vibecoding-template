#!/bin/sh
# Pre-push hook — 推送前检查
# 复制到 .git/hooks/pre-push 启用

if [ "$SKIP_VIBE_GATE" = "1" ]; then
  exit 0
fi

echo "Running vibecoding quality gate before push..."
node .vibecoding/quality-gate.js
exit $?