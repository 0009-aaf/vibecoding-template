#!/bin/sh
# Pre-commit hook — 运行质量闸门
# 复制到 .git/hooks/pre-commit 启用

if [ "$SKIP_VIBE_GATE" = "1" ]; then
  exit 0
fi

echo "Running vibecoding quality gate..."
node .vibecoding/quality-gate.js
exit $?