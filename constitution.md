# 项目宪法 — vibecoding-template

> 最高约束：所有 AI 命令（vibe-plan/spec/implement/audit）与人工提交都必须遵守，**违背任意一条即否决**。
> 条款全部提炼自既有约定（AGENTS.md / DoD / 反合理化表），本文件只做归拢不做发明。
> 修改任何条款须先在 `docs/05-DECISIONS.md` 记 ADR（见 C13）。

## 不可协商条款

| # | 条款 | 违背的典型表现 |
|---|---|---|
| C1 | 入口判空：函数/脚本/接口入口对 null / undefined / [] / "" 四种空值防御 | "传进来肯定有值" |
| C2 | 精准修改：只改任务点名的文件；注意到的问题记入 `docs/TECH-DEBT.md`（NOTICED BUT NOT TOUCHING），不当场动手 | 顺手重构旁边文件 |
| C3 | catch 必处理：捕获的异常要么处理、要么显式 re-throw（带 cause） | 空 catch 块 |
| C4 | 异常不吞：失败必须有可观测输出（日志/退出码/报告），禁止静默降级 | "失败就失败吧，不阻断" |
| C5 | 密钥永不入库：`.env` 不入库、密钥不进前端 bundle、含密钥的本地配置（如 opencode.json）不入仓 | 测试密钥提交"先用着" |
| C6 | 库清单即边界：只用 `docs/02-ARCHITECTURE.md` §6 清单内的库；新库先改清单（记 ADR）再写代码；禁止自造轮子 | "这个轮子我写一个只要 20 行" |
| C7 | 测试必须红→绿：新行为先写失败测试再实现；"完成了只是还没运行过"不算完成 | 测试后补、或只跑编译不跑运行 |
| C8 | Protected Region 不可碰：`docs/02-ARCHITECTURE.md` §5 声明的文件禁止修改（首次创建除外） | "改一行没事" |
| C9 | 不适用必须带理由：写成"不适用(理由)"，禁止静默跳过任何必答项 | 表格里留空 |
| C10 | 文档未同步不算完成：代码变更与契约/STATUS/CHANGELOG/TECH-DEBT 同步落盘 | "文档回头补" |
| C11 | 防御机制必测：任何守卫/门禁/拦截机制配误报/漏报矩阵并实测通过才算就位 | 只写不测的拦截 |
| C12 | 契约即团队边界：函数签名与 API 响应必须匹配 `docs/04-CONTRACTS.md`；冲突先改契约（记 ADR）再改代码 | 实现自由发挥、测试跟着实现走 |
| C13 | 宪法变更须 ADR：修改本文件任何条款，先在 `docs/05-DECISIONS.md` 记 ADR 再生效 | 悄悄放宽条款 |

## 与其他约束文件的关系

| 文件 | 分工 |
|---|---|
| `constitution.md`（本文件） | 不可协商底线，违背即否决 |
| `docs/DoD.md` | per-project 完成清单（宪法管"不能做"，DoD 管"没做完"）；无切片交付的仓库允许缺席（见 `docs/00-DOC-STANDARD.md` §1，底线由本文件承载） |
| `docs/04-CONTRACTS.md` | 跨端契约边界（C12 的依据）；无跨端契约的项目允许缺席（见 `docs/00-DOC-STANDARD.md` §1） |
| `docs/08-CODING-STANDARDS.md` | 可协商的项目级风格（冲突时宪法 > DoD > 本文件） |
| `AGENTS.md` | AI 行为入口约定，引用本文件 |

## 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-08-23 | 初版：13 条款自 AGENTS/DoD/反合理化表提炼归拢 | 项目宪法引入（ADR-005，参照 Spec Kit constitution 实践） |
